= simple_form_for @request do |f|
  = f.collection_select :team_member_id, TeamMember.all, :id, :full_name, { prompt: "Choose a member" }
  = f.input :description
  = f.input :number_of_trucks
  = f.input :address
  = f.input :date, as: :datetime
  = f.button :submit